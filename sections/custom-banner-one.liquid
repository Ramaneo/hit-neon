<section id="section-{{ section.id }}" class="section-banner-one">
  {% comment %}
    <custom-banner-container>
      <button-wrapper-cbc>
        <button class="one">Create Your Design</button>
        <button class="two">Upload Your Design/Logo</button>
      </button-wrapper-cbc>
    </custom-banner-container>

    <button-wrapper-cbc-mobile>
      <button class="one">Create Your Design</button>
      <button class="two">Upload Your Design/Logo</button>
    </button-wrapper-cbc-mobile>
  {% endcomment %}

  {% comment %}
    <video autoplay loop muted playsinline class="background-video">
      <source src="https://cdn.shopify.com/videos/c/o/v/0e1a2ddd85844907824fedb0693d7388.mp4" type="video/mp4">
      Your browser does not support the video tag.
    </video>
  {% endcomment %}
  {% comment %}
    <img
      src="https://cdn.shopify.com/s/files/1/0626/5992/0039/files/Hit-Neon-Website-Banner_V2_1.jpg?v=1718119240"
      class="background-image"
      alt="Background Image"
    >
  {% endcomment %}

  <custom-banner-container>
    <a href="/products/create-your-own-custom-neon-sign" class="neon-button2">Create Your Design</a>
    <a href="/pages/custom-designed-led-neon-sign" class="neon-button">Upload Your Design/Logo</a>
  </custom-banner-container>
</section>
<script>
  document.addEventListener('DOMContentLoaded', function () {
    var video = document.querySelector('.background-video');
    if (video) {
      var playVideo = function () {
        video.play().catch(function (error) {
          console.log('Autoplay was prevented.');
        });
      };
      window.addEventListener('scroll', playVideo, { once: true });
      window.addEventListener('touchstart', playVideo, { once: true });
      window.addEventListener('click', playVideo, { once: true });
    }
  });
</script>

<style>
  {% comment %} custom-banner-container {
    width: 100vw;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 720px;
    background-image: url("https://cdn.shopify.com/s/files/1/0484/1429/4167/files/NeonSummerSP-01_1.png?v=1710921244");
    background-size: cover;
    background-repeat: no-repeat;
    margin-bottom: 40px;

  } {% endcomment %}
  {% comment %}

  button-wrapper-cbc,button-wrapper-cbc-mobile {
     display: flex;
     justify-content: center;
     align-items: center;
  }

  button-wrapper-cbc button,button-wrapper-cbc-mobile button{
     width: 590.633px;
     height: 73.489px;
     cursor: pointer;
     font-family: "montserrat-bold";
     display: flex;
     justify-content: center;
     align-items: center;
     cursor: pointer;
     background-color: #fff;
     outline: none;
       border: none;
      /* Typography */

      color: #333;
      text-align: center;
      font-size: 20px;
      font-style: normal;
      font-weight: 700;
      line-height: 20px;

  }
  button-wrapper-cbc button:hover,button-wrapper-cbc-mobile button:hover {
      background-color: palevioletred;
      color: #fff;
  }

  button-wrapper-cbc .one {
     margin-right: 40px;

  }


  @media (max-width: 1300px) {

      button-wrapper-cbc  {
          flex-direction: column;
      }
      button-wrapper-cbc-mobile {
          flex-direction: column;
      }

      button-wrapper-cbc .one,button-wrapper-cbc-mobile .one {
          margin-right: 0px;
          margin-bottom: 20px;
      }
  }

  @media (max-width: 641px) {

      button-wrapper-cbc {
          display: none;
      }

      button-wrapper-cbc-mobile {
          flex-direction: column;
          margin: auto;
          margin-bottom: 20px;
      }

      button-wrapper-cbc-mobile .one {
          margin-right: 0px;
          margin-bottom: 20px;
      }
      button-wrapper-cbc-mobile button {
          font-size: 14px;
          width: 80%;
          height: 40px;
          font-family: "montserrat-regular";
          background-color: #fff;
          border: 1px solid #333;
      }
      button-wrapper-cbc-mobile button:hover {
          border: none;
      }
  }

  @media (min-width: 641px) {

    button-wrapper-cbc-mobile {
      display: none;
    }


  }   {% endcomment %}

  @media (min-width: 641px) {
    .background-video {
      position: absolute;
      left: 0;
      width: 100%;
      object-fit: cover;
      z-index: -1;
      height: 640px;
    }
    .section-banner-one {
      {% comment %} height: 640px; {% endcomment %}
       display: flex; justify-content: center;
    }
    .neon-button::before {
      content: "";
      background: linear-gradient(
        90deg,
        var(--orange),
        var(--yellow),
        var(--green),
        var(--blue),
        var(--purple),
        var(--pink),
        var(--red)
      );
      height: 30%;
      width: 60%;
      position: absolute;
      bottom: -20%;
      z-index: -1;
      background-size: 200%;
      animation: animate 2s infinite linear;
      filter: blur(calc(0.8 * var(--m)));

    }
    .neon-button2::before {
      content: "";
      background: linear-gradient(
        90deg,
        var(--orange),
        var(--yellow),
        var(--yellowish),
        var(--yellow),
        var(--yellowish),
        var(--yellow),
        var(--orange)
      );
      height: 30%;
      width: 60%;
      position: absolute;
      bottom: -20%;
      z-index: -1;
      background-size: 200%;
      animation: animate 2s infinite linear;
      filter: blur(calc(0.8 * var(--m)));

    }
  }

  @media (min-width: 2500px) {
    .background-video {
      position: absolute;
      left: 0;
      width: 100%;
      object-fit: cover;
      z-index: -1;
      height: 1000px;
    }
    .section-banner-one {
      height: 1000px; display: flex; justify-content: center;
    }
  }



  :root {
      /*  change this for scaling  */
      --m: 4rem;

      --red: #ff6565;
      --pink: #ff64f9;
      --purple: #6b5fff;
      --blue: #4d8aff;
      --green: #5bff89;
      --yellow: #ffee55;
      --orange: #ff6d1b;
      --yellowish: #E8D5AB;
    }
    /* body {
        background-color: #141516;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      } */

      @media (max-width: 641px) {
        custom-banner-container {
          flex-direction: column !important;
          padding: 20px;
          width: 100% !important;

        }
        .neon-button {
          width: 100% !important;
        }
        .neon-button2 {
          width: 100% !important;
        }
        .background-video {
          position: absolute;
          left: 0;
          width: 100%;
          height: 100%;
          object-fit: cover;
          z-index: -1;
          height: 640px;
        }
        .section-banner-one {
          height: 640px; display: flex; justify-content: center;
        }
      }


      custom-banner-container {
        {% comment %} background-color: #141516; {% endcomment %}
          {% comment %} width: 100vw; {% endcomment %}
          display: flex;
          justify-content: center;
          flex-direction: row;
          align-items: center;
          {% comment %} height: 720px; {% endcomment %}
          margin-bottom: 40px;
          margin-top: auto;
          position: sticky;
          {% comment %} padding: 0px 50px; {% endcomment %}
        gap: 20px;
        {% comment %} overflow: hidden; {% endcomment %}

        width: 100vw;
    {% comment %} height: 720px; {% endcomment %}

  /* Background image */
  background-image: url("https://cdn.shopify.com/s/files/1/0626/5992/0039/files/Hit-Neon-Website-Banner_V2_1.jpg?v=1718119240");
  background-size: cover;
  background-repeat: no-repeat;
  height: 650px;
  padding-bottom: 50px;
        }

    .neon-button {
      text-decoration: none;
      border: calc(0.08 * var(--m)) solid transparent;
      position: relative;
      color: #f3f3f3;
      font-family: "montserrat-bold";
      {% comment %} font-size: var(--m); {% endcomment %}
      font-size: 20px;

      border-radius: calc(0.7 * var(--m));
      padding: calc(0.5 * var(--m)) calc(1 * var(--m));
      display: flex;
      justify-content: center;
      cursor: pointer;
      {% comment %} flex: 1; {% endcomment %}
      width: 500.633px;
      margin-top: auto;


      background: linear-gradient(#121213, #121213),
        linear-gradient(#121213 50%, rgba(18, 18, 19, 0.6) 80%, rgba(18, 18, 19, 0)),
        linear-gradient(
          90deg,
          var(--orange),
          var(--yellow),
          var(--green),
          var(--blue),
          var(--purple),
          var(--pink),
          var(--red)
        );
      background-origin: border-box;
      background-clip: padding-box, border-box, border-box;
      background-size: 200%;
      animation: animate 2s infinite linear;
    }
    .neon-button2 {
      margin-top: auto;

      text-decoration: none;
      border: calc(0.08 * var(--m)) solid transparent;
      position: relative;
      color: #f3f3f3;
      font-family: "montserrat-bold";
      {% comment %} font-size: var(--m); {% endcomment %}
      font-size: 20px;

      border-radius: calc(0.7 * var(--m));
      padding: calc(0.5 * var(--m)) calc(1 * var(--m));
      display: flex;
      justify-content: center;
      cursor: pointer;
      {% comment %} flex: 1; {% endcomment %}
      width: 500.633px;

      background: linear-gradient(#121213, #121213),
        linear-gradient(#121213 50%, rgba(18, 18, 19, 0.6) 80%, rgba(18, 18, 19, 0)),
        linear-gradient(
          90deg,
          var(--orange),
        var(--yellow),
        var(--yellowish),
        var(--yellow),
        var(--yellowish),
        var(--yellow),
        var(--orange)
        );
      background-origin: border-box;
      background-clip: padding-box, border-box, border-box;
      background-size: 200%;
      animation: animate 2s infinite linear;
    }



    .neon-button:hover,
    .neon-button:hover::before {
      animation: animate 0.5s infinite linear;
    }
    .neon-button2:hover,
    .neon-button2:hover::before {
      animation: animate 0.5s infinite linear;
    }

    @keyframes animate {
      0% {
        background-position: 0;
      }
      100% {
        background-position: 200%;
      }
    }

    @media screen and (max-width: 1000px) {
      :root {
        --m: 2rem;
      }
    }
</style>
